{
  "name": "jsonbuilder",
  "version": "1.0.0",
  "description": "creates an object blueprint and allow for creation of said objects, writing them to a JSON file",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "floseph",
  "license": "ISC",
  "dependencies": {
    "readline-sync": "^1.4.10"
  }
}
